#include <stdio.h>
#include <stdlib.h>

int** recognize(int *array) {
	int** holder = malloc(sizeof(int*)*2);
	int avg[11];
	int *hold = calloc(sizeof(int)*11);
	for (j=0;j<11;j++) {
		array[j] += hold[j]; 
	}

		for (k=0;k<11;k++) {
			avg[k] = 5*hold[k] + somearray[i].array[k];
		}
	}

	holder[0] = avg;
	holder[1] = array;
	return avg;
}
